fastapi>=0.110
# Used directly (from pydantic import BaseModel) in api.py/core.py — FastAPI also
# pulls it in, but we import it ourselves so declare it explicitly.
pydantic>=2.0
uvicorn[standard]>=0.27
qiskit>=1.0
numpy>=1.24
# Quantum-run feature (QCB_ENABLE_QUANTUM_HW): local Aer sampler for the
# counts-only path, and Qiskit Runtime for real IBM Quantum devices.
qiskit-aer>=0.14
qiskit-ibm-runtime>=0.20
# AWS Braket provider (QCB_QUANTUM_PROVIDER=braket). Optional and lazily imported
# — only needed to run on Braket's local simulator or real AWS devices. Uncomment
# (or `pip install amazon-braket-sdk`) to enable:
# amazon-braket-sdk>=1.80
# AI circuit-explainer feature (QCB_ENABLE_AI). Only needed when the explainer
# is enabled; lazily imported so the app runs without it otherwise.
anthropic>=0.40
# Tutor memory features (learner profiles, sessions, quizzes). Optional: the
# core playground runs without a database — backend/db.py imports psycopg lazily
# and degrades gracefully when it's absent or no QCB_DATABASE_URL is set. The
# [binary] extra pulls in a prebuilt libpq so no system Postgres dev headers are
# needed.
psycopg[binary]>=3.1
# Schema migrations for the memory tables (`make migrate`). Migrations are plain
# SQL run through Alembic's versioning; no ORM models.
alembic>=1.13
# Semantic recall for the tutor's long-term memory (pgvector + hybrid retrieval).
# Lazily loaded — the app runs without it; only interaction embeddings and
# semantic search are unavailable when the package is absent or the model
# hasn't been downloaded yet. The model (~90 MB, all-MiniLM-L6-v2) is cached
# by sentence-transformers on first use.
sentence-transformers>=2.7
# Test suite (`make test`). Dev-only; not needed to run the app.
pytest>=8.0
